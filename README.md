ERP Pro System - Enterprise EditionAn advanced, monolithic Enterprise Resource Planning (ERP) system built with pure, modern PHP 8.1+ using Clean Architecture and Domain-Driven Design (DDD) principles.FeaturesMulti-Tenant Architecture: Secure data isolation by Company and Branch.Robust Authentication & Authorization: RBAC (Role-Based Access Control) with granular permissions, API Keys, and SAML 2.0 SSO readiness.Financial Integrity: Strict Double-Entry Bookkeeping with automated Journal Entries.Complete Business Modules:Administration & SecurityAccounting & FinancePurchasing & Supply ChainSales & CRMInventory & WarehousingHuman Resources & PayrollMaintenance & Fleet ManagementPoint of Sale (POS)System RequirementsPHP >= 8.1MySQL >= 8.0 or MariaDB >= 10.5PDO, JSON, OpenSSL, and MBString PHP extensionsInstallationClone the repository.Run composer install --optimize-autoloader --no-dev.Copy .env.example to .env and configure your database credentials.Set folder permissions for storage/ and sessions/ to 0755 or 0775.Run the migrations and seeders:php bin/console migrate
Point your web server's Document Root to the public/ directory.ArchitectureThe system is divided into bounded contexts (Modules) inside the app/Modules directory. It uses a custom Dependency Injection Container, a proprietary Router, and a custom ORM-like Query Builder for maximum performance and security without heavy third-party framework overhead.